export interface Totals {
    total: number,
    id: string
}

export interface SalesSummary {
    conteo: number,
    total: number,
    month: string,
    date: string,
    outflow: number,
    transfer: number,
    totalNeto: number
}